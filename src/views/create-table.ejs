<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Custom Database Engine</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet">
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary" aria-label="Main navigation">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">
                <i class="bi bi-database"></i> Custom Database Engine
            </a>
            <div class="navbar-nav me-auto">
                <a class="nav-link" href="/"><i class="bi bi-house"></i> Home</a>
                <a class="nav-link active" href="/create-table"><i class="bi bi-plus-circle"></i> Create Table</a>
                <a class="nav-link" href="/query"><i class="bi bi-code"></i> SQL Query</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">
                        <h4 class="mb-0">
                            <i class="bi bi-plus-circle"></i> Create New Table
                        </h4>
                    </div>
                    <div class="card-body">
                        <form method="POST" action="/create-table">
                            <div class="mb-3">
                                <label for="tableName" class="form-label">Table Name</label>
                                <input type="text" class="form-control" id="tableName" name="tableName" required
                                       pattern="[a-zA-Z_][a-zA-Z0-9_]*" 
                                       title="Table name must start with a letter or underscore, followed by letters, numbers, or underscores">
                                <div class="form-text">Table name must be unique and follow naming conventions.</div>
                            </div>

                            <div class="mb-3">
                                <label for="columns" class="form-label">Columns</label>
                                <textarea class="form-control" id="columns" name="columns" rows="6" required
                                          placeholder="name:VARCHAR, age:NUMBER, email:VARCHAR, active:BOOLEAN"></textarea>
                                <div class="form-text">
                                    Define columns in format: <code>column_name:TYPE</code>, separated by commas.<br>
                                    Supported types: <code>VARCHAR</code>, <code>NUMBER</code>, <code>BOOLEAN</code>, <code>TEXT</code>
                                </div>
                            </div>

                            <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                                <a href="/" class="btn btn-secondary">Cancel</a>
                                <button type="submit" class="btn btn-primary">
                                    <i class="bi bi-plus-circle"></i> Create Table
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

                <!-- Examples -->
                <div class="card mt-4">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <i class="bi bi-lightbulb"></i> Examples
                        </h5>
                    </div>
                    <div class="card-body">
                        <h6>Users Table:</h6>
                        <div class="bg-light p-3 rounded mb-3">
                            <strong>Table Name:</strong> <code>users</code><br>
                            <strong>Columns:</strong> <code>name:VARCHAR, email:VARCHAR, age:NUMBER, active:BOOLEAN</code>
                        </div>

                        <h6>Products Table:</h6>
                        <div class="bg-light p-3 rounded mb-3">
                            <strong>Table Name:</strong> <code>products</code><br>
                            <strong>Columns:</strong> <code>name:VARCHAR, price:NUMBER, description:TEXT, category:VARCHAR</code>
                        </div>

                        <h6>Blog Posts Table:</h6>
                        <div class="bg-light p-3 rounded">
                            <strong>Table Name:</strong> <code>posts</code><br>
                            <strong>Columns:</strong> <code>title:VARCHAR, content:TEXT, author:VARCHAR, published:BOOLEAN, created_date:VARCHAR</code>
                        </div>
                    </div>
                </div>

                <!-- SQL Alternative -->
                <div class="card mt-4">
                    <div class="card-header">
                        <h5 class="mb-0">
                            <i class="bi bi-code"></i> SQL Alternative
                        </h5>
                    </div>
                    <div class="card-body">
                        <p>You can also create tables using SQL syntax in the <a href="/query">Query Interface</a>:</p>
                        <div class="bg-dark text-light p-3 rounded">
                            <code>
                                CREATE TABLE users (<br>
                                &nbsp;&nbsp;name VARCHAR(255),<br>
                                &nbsp;&nbsp;email VARCHAR(255),<br>
                                &nbsp;&nbsp;age NUMBER<br>
                                );
                            </code>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
